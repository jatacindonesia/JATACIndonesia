rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global allow read and write to bypass iframe authentication limits
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
